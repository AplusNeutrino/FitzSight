# FitzSight v0.12.0 Validation Record

**Date:** 2026-08-12  
**Baseline:** `AplusNeutrino/FitzSight` main commit `272dc3d5dc180bced000bcac289358dba12fdd08` (`Release FitzSight v0.11.0`)  
**Delivery status:** local v0.12.0 delivery built; not yet published to GitHub by the user.

## 1. Build / test validation

The combined pytest process can exceed the build sandbox command ceiling, so the full suite was executed as complete **non-overlapping test-file groups** and then aggregated.

```text
85 tests collected
84 passed
1 skipped
0 failed
python -m compileall -q src scripts streamlit_app.py : PASS
```

The one skip is the DuckDB-specific build-sandbox integration test because DuckDB is not installed in this sandbox. It does not supersede prior deployment-runtime DuckDB evidence and is not presented as a new live DuckDB PASS.

After the v0.12 final-machine / handoff / preflight version updates, the affected targeted regression set was rerun:

```text
tests/test_submission_preflight.py
+ tests/test_v10_manual_handoff.py
+ tests/test_v11_final_machine.py
= 12 passed
```

This targeted rerun overlaps the 85-test suite and therefore is **not added** to the aggregate count.

## 2. Fixed benchmark and adversarial release gate

Runtime evidence:

- `docs/V0.12_BENCHMARK_RESULTS.json`
- `docs/V0.12_ADVERSARIAL_RESULTS.json`

```text
fixed deterministic benchmark:          5 / 5 PASS
root-cause scenario accuracy:           100%
false-correlation rejection accuracy:   100%
mean evidence coverage:                 100%
verifier violations:                    0
adversarial release gate:               8 / 8 PASS
```

These are synthetic deterministic evaluation results, not production customer-outcome claims.

## 3. Evaluation v2 — holdout robustness

Runtime evidence: `docs/V0.12_HOLDOUT_RESULTS.json`.

The showcase seed `20260811` is excluded. Holdout seeds `20260821` and `20260831` test CRM/FTD and false-correlation question paraphrases.

```text
case runs:                               8
intent routing stability:               100%
verification pass rate:                 100%
mean evidence coverage:                 100%
false-correlation refusal correctness:  100%
supported-candidate rate:                75%
```

The 75% supported-candidate rate is intentionally retained: one unseen CRM seed did not support the candidate strongly enough and FitzSight returned `insufficient_evidence` rather than forcing a root-cause conclusion.

## 4. Evaluation v2 — controlled architecture ablation

Runtime evidence: `docs/V0.12_ABLATION_RESULTS.json`.

This is a deterministic architecture experiment, **not** a Generic LLM or LLM+SQL baseline. The same six fixtures are compared under:

1. Full FitzSight verifier/evidence gate.
2. A controlled passthrough ablation with that final gate removed.

Key result:

```text
Full FitzSight adversarial refusal correctness:       100%
Full FitzSight unsafe-answer rate on adversarial:       0%
No-verifier-gate adversarial refusal correctness:       0%
No-verifier-gate unsafe-answer rate on adversarial:   100%
```

No live-provider baseline is reported because no such provider experiment was run.

## 5. Bounded-adaptive hero and product-process evidence

Runtime-derived evidence:

- `docs/V0.12_HERO_RUN.json`
- `submission/FitzSight_Hero_Run_Evidence.html`
- `submission/FitzSight_Hero_Run_Evidence.png`

Verified hero result:

```text
intent:                  crm_routing_ftd_investigation
verification_passed:     true
root_cause_status:       supported_candidate
document_source_ref:     CRM-CHANGE-2026-0715#p1
execution_trace_steps:   9
follow_up_status:        verified_with_guardrail
```

A tested event-tool dependency failure records the failure and degrades the root-cause status to `insufficient_evidence`; the Verifier then emits only a bounded evidence-supported answer.

## 6. Local handoff / final-machine evidence

Local-only release checks were run without external account writes:

```text
submission preflight:             PASS
handoff ready_for_user_takeover:  true
final-machine local_core_ready:   true
deterministic Agent smoke:        verified
```

Artifacts:

- `docs/V0.12_SUBMISSION_PREFLIGHT.json`
- `docs/V0.12_HANDOFF_READINESS.json`
- `docs/V0.12_FINAL_MACHINE_READINESS.json`
- `submission/FitzSight_Manual_Handoff.zip`
- `submission/FitzSight_Final_Machine_Kit.zip`

The manual handoff and final-machine kit were both ZIP-integrity checked during the local build. Their delivery-time hashes are recorded by the outer delivery packet rather than embedded here, so rebuilding a self-contained kit cannot make its own documentation stale.

## 7. Explicitly unverified / unfinished

These items are **not** converted into PASS by local code presence, mocks, compilation or deterministic fallback:

- Streamlit live health-check on the final presentation machine: **unverified in this sandbox**.
- OpenAI Responses live planner/provider latency/tokens: **not requested / unverified**.
- Formal v0.12 PPTX/PDF regeneration from the new one-hero + one-refusal content source: **in progress**.
- Final GOAI handbook reviewer gate: **todo until formal deck/full asset synchronization completes**.
- GOAI portal review/upload/final submit and Gmail/confirmation evidence: **user-manual only; not performed**.
- Human pitch/demo/Q&A rehearsal evidence: **requires a real user rehearsal; not inferred from scripts**.

## 8. Release interpretation

v0.12.0 closes the competition-alignment **core implementation/evaluation** work that has direct code or reproducible evidence. It deliberately does not close presentation/runtime/external tasks whose Definition of Done requires a real generated deck, final-machine live UI/provider evidence, portal confirmation or human rehearsal.
