# FitzSight v0.8 Validation

**Date:** 2026-08-11  
**Theme:** Initial-round submission sprint

## Code / test validation

The test suite now collects **59 tests**. A single full `pytest -q` process exceeded the build sandbox time ceiling, so the complete suite was executed in non-overlapping groups.

```text
Group 1: 21 passed
Group 2: 14 passed
Group 3: 20 passed, 1 skipped
Submission-asset tests: 3 passed
--------------------------------
Aggregate: 58 passed, 1 skipped
```

The only skipped test is the DuckDB-specific build-sandbox integration test. DuckDB was already validated separately in the deployment environment on 2026-08-11 using `data/generated`, with both the constrained planner and JSON-file planner returning final status `verified`.

Compile validation:

```text
python -m compileall -q src scripts tests streamlit_app.py
PASS
```

## Benchmark / adversarial validation

Five-scenario deterministic benchmark on the SQLite build fallback:

```text
scenario_count:                       5
passed:                               5
failed:                               0
scenario_pass_rate:                   1.0
root_cause_scenario_accuracy:         1.0
false_correlation_rejection_accuracy: 1.0
mean_evidence_coverage:               1.0
total_verifier_violations:            0
```

Eight-case adversarial release gate:

```text
case_count:                            8
passed:                                8
failed:                                0
overall_adversarial_pass_rate:         1.0
scope_refusal_accuracy:                1.0
planner_policy_violation_catch_rate:   1.0
verifier_integrity_catch_rate:         1.0
causal_overclaim_catch_rate:           1.0
ground_truth_leak_catch_rate:          1.0
false_correlation_rejection_rate:      1.0
```

## One-command launcher validation

`start_demo.py` was tested in explicit CLI, auto-fallback, and mocked UI command-construction paths.

Actual deterministic CLI launch in the build environment:

```text
python scripts/start_demo.py --mode cli --backend sqlite
```

completed successfully and contained final answer status:

```text
verified
```

Because Streamlit is not installed in the build sandbox, `--mode auto` correctly selects the deterministic CLI fallback. This is intentional competition-safe behavior and is not a Streamlit runtime validation.

## Submission deck validation

Generated assets:

```text
submission/FitzSight_GOAI_Initial_Round.pptx
submission/FitzSight_GOAI_Initial_Round.pdf
```

The deck contains **12 slides**. PDF export used LibreOffice from the same PPTX.

PDF inspection:

```text
Pages:      12
Page size:  960 x 540 pt equivalent / 16:9
Encrypted:  False
```

The PDF was rendered to PNG at 144 DPI and visually reviewed. No clipping, overlap, or broken glyphs were observed after the title-slide correction.

Asset hashes at v0.8 build time:

```text
PPTX SHA-256: 25e11a245cc09017e7a6f710f605cf13a59c457711e19dc900773fc8118553f1
PDF  SHA-256: 41e58215828281891c6138d18a379926f4ad111efb1b599754db656626f43421
```

## Submission preflight

```text
passed: true
missing required files: 0
generated CSV files packaged: 0
secret hits: 0
submission presentation assets: 2
```

See `docs/V0.8_SUBMISSION_PREFLIGHT.json`.

## External runtime evidence still pending

The following are implemented but remain **not live-validated** under the project's external-runtime evidence rule:

- Streamlit runtime on the final demo/presentation environment;
- OpenAI Responses planner with real user credentials and a live accessible model.

Their status must not be changed to `done` until actual runtime output is supplied.
