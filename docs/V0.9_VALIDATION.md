# FitzSight v0.9 Validation Evidence

Date: 2026-08-11

## Scope

v0.9 freezes the five-intent analytical core and strengthens the competition runtime/submission path: pure presentation logic, offline demo/video fallback, runtime diagnostics, live-runtime validation commands, provider telemetry, deterministic latency measurement, dynamic pitch-deck metrics, and a final submission preflight.

## Automated tests

Collection:

```text
69 tests collected
```

The suite was executed in two complete, non-overlapping groups because the build sandbox has a strict single-process time ceiling:

```text
Group 1: 31 passed
Group 2: 37 passed, 1 skipped
Aggregate: 68 passed, 1 skipped
```

The only skip is the DuckDB-specific integration test in this build environment. DuckDB was separately validated in the deployment environment on 2026-08-11 with `data/generated`; the default constrained planner and JSON-file planner both returned a verified result.

Compile validation:

```text
python -m compileall -q src scripts tests streamlit_app.py
PASS
```

## Five-scenario deterministic benchmark

`docs/V0.9_BENCHMARK_RESULTS.json`:

```text
scenario_count:                       5
passed:                               5
failed:                               0
scenario_pass_rate:                   100%
root_cause_scenario_accuracy:         100%
false_correlation_rejection_accuracy: 100%
mean_evidence_coverage:               100%
total_verifier_violations:            0
```

## Adversarial release gate

`docs/V0.9_ADVERSARIAL_RESULTS.json`:

```text
cases:                                8
passed:                               8
failed:                               0
overall_adversarial_pass_rate:        100%
```

The cases cover unsupported trading/AML actions, planner SQL injection, unknown high-impact actions, missing evidence, causal overclaim, evaluation-only ground-truth leakage, and the false-correlation benchmark.

## Current fixed-seed submission metrics

Competition-facing demo metrics were regenerated from fresh verified deterministic Agent runs rather than copied from historical release constants.

```text
CRM / FTD
  affected:           -7.53 pp
  control:            -1.21 pp
  response median:   +29.15 min

Net deposits
  net change:        -$187.8k
  deposits:           +$59.2k
  withdrawals:       +$246.9k
  top-11 share:        91.6%

Customer Intelligence
  Europe customers:    6,770
  High Value users:      3.7%
  High Value deposits:   53.7%

Marketing quality
  lead volume:          +315%
  aggregate FTD:      -10.84 pp
  Paid Search FTD:    -16.44 pp

False correlation
  Asia FTD:            -8.13 pp
  Affiliate FTD:      -15.81 pp
  nearby office event supported as cause: false
```

## UI presentation layer

`tests/test_ui_presenter.py` and the v0.9 pitch/runtime tests validate that all five verified Agent workflows produce:

- KPI cards;
- a chart specification;
- investigation-plan trace rows;
- Evidence cards;
- JSON-safe presentation output.

The pure presenter performs no SQL/statistical business calculation. `streamlit_app.py` is a renderer over this verified presentation data.

## Offline demo fallback

`submission/FitzSight_Offline_Demo.json` and `.html` were regenerated from five actual verified deterministic Agent runs:

```text
scenario_count:   5
verified_runs:    5
evidence_records: 67
```

The MP4 backup was generated from the verified offline-demo JSON and inspected with FFprobe:

```text
codec:       H.264
resolution:  1280x720
frame rate:  30 fps
duration:    64 seconds
```

Representative intro/middle/end frames were visually reviewed after the final generation and showed no observed clipping or overlap.

## Pitch deck / PDF

`scripts/build_pitch_deck.py` now creates Slides 4-8 from fresh verified runtime metrics in a temporary synthetic-data directory. Speaker notes are generated from the same source.

Final assets:

```text
PPTX SHA-256: d37349c51ff115d7d890a8b49a8596cb058df7e88d517a1559ec392de112378c
PDF  SHA-256: 90628f14c36ceb73de6b50bebea1efe5e0097593a7d5c51a38bc3e4390d3f6b4
```

The PDF was exported from the same PPTX through LibreOffice and rendered at 200 DPI for visual review:

```text
pages:        12
page size:    960 x 540 pt (16:9)
fonts:        embedded
clipping:     none observed
overlap:      none observed
broken glyph: none observed
```

## Deterministic latency

`docs/V0.9_DETERMINISTIC_LATENCY.json` records 15 verified end-to-end SQLite runs in this build environment:

```text
overall mean: 292.29 ms
p50:          300.90 ms
p95:          343.35 ms
```

This is an environment-specific deterministic measurement. Live model-provider latency and monetary cost are not reported without a real provider call.

## External runtime evidence

`docs/V0.9_RUNTIME_STATUS.json`, `docs/V0.9_STREAMLIT_RUNTIME_CHECK.json`, and `docs/V0.9_OPENAI_RUNTIME_CHECK.json` explicitly record the current build environment:

```text
core deterministic demo: READY
Streamlit live runtime:  NOT VALIDATED (dependency unavailable)
OpenAI live planner:     NOT VALIDATED (model/key not configured)
secrets disclosed:       false
```

These states remain open in the project tracker; offline artifacts or mocks do not substitute for live evidence.

## Submission preflight

`docs/V0.9_SUBMISSION_PREFLIGHT.json`:

```text
passed:                  true
missing required files:  0
generated CSV files:     0
secret hits:             0
offline demo verified:   5 / 5
upload ZIP integrity:    PASS
```

The preflight is local evidence only. Actual GOAI portal upload and confirmation remain user-controlled external actions.

## Final targeted regression after packaging edits

After the final upload-bundle/preflight/pitch-metric edits, the directly affected regression set was re-run:

```text
10 passed
```

This covered submission preflight policy, offline/runtime/upload assets, OpenAI/runtime safety helpers, and dynamic pitch-metric synchronization. A final compileall pass also succeeded.
