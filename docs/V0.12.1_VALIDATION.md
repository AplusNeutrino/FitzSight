# FitzSight v0.12.1 Validation

## Release scope

v0.12.1 is a judge-facing formal-deck / competition-asset synchronization patch on the published v0.12.0 implementation baseline (`04f196e74495d345fa7bc979d7447070c56e4020`). It adds no new business intent and does not widen planner, SQL, tool or financial-decision authority.

## Test suite

The suite was executed in complete non-overlapping groups because long combined pytest processes can exceed the build-sandbox command ceiling. Collection completed successfully after the repository root was added to pytest's local import path for evaluation-script tests.

```text
87 tests collected
86 passed
1 skipped
0 failed
compileall: PASS
```

The single skip is the DuckDB-specific build-environment integration test. Prior deployment-runtime DuckDB evidence remains separate; this release does not convert a skip into a live PASS.

## Formal deck / PDF validation

- `scripts/build_pitch_deck.py`: PASS
- PPTX slide count: **12**
- same-source PDF export: **12 pages**
- `slides_test.py`: **PASS — no canvas overflow detected**
- PDF rendered to page images and visually reviewed: **PASS**
- main narrative: **one CRM/FTD hero + one false-correlation refusal**
- runtime-derived hero trace/answer evidence included; other workflows are breadth proof

Asset hashes at validation time:

```text
PPTX  6ede0712f14642aee88e10b9e3ff56e9413ce36b45ea12f53c4328ee4298090a
PDF   914da2589b253efcaf2e35872009c13efee8e1209ef0698f355e941ad904caab
```

## Deterministic release gates

Fresh v0.12.1 regression outputs:

```text
fixed benchmark:                    5 / 5 PASS
root-cause scenario accuracy:       100%
false-correlation rejection:        100%
mean evidence coverage:             100%
verifier violations:                0

adversarial release gate:          8 / 8 PASS
scope refusal:                      100%
planner-policy catch:               100%
verifier-integrity catch:           100%
causal-overclaim catch:             100%
ground-truth leak catch:            100%
false-correlation rejection:        100%
```

Evaluation v2 remains the checked-in v0.12 protocol/results because the analytical core is unchanged:

```text
holdout runs routed + verified:     8 / 8
holdout routing stability:          100%
holdout evidence coverage:          100%
false-correlation refusal:           100%
supported-candidate rate:            75%

Full FitzSight adversarial refusal
in controlled architecture ablation: 100%
No-verifier/evidence-gate unsafe
answer rate on adversarial fixtures:  100%
```

The 75% supported-candidate rate is retained because one unseen CRM seed correctly returns `insufficient_evidence`; it is not rewritten as a root-cause success. The ablation is not described as a Generic LLM baseline.

## GOAI reviewer gate

`docs/V0.12.1_GOAI_REVIEWER_GATE.md` audits the formal package against the official scoring dimensions and AI+Finance personalized checks. It explicitly keeps current implementation separate from planned enterprise RBAC/PII/retention controls.

## Local operator gates

After the synchronized packages are rebuilt, the release requires:

```text
submission preflight:             PASS
handoff ready_for_user_takeover:  true
final-machine local_core_ready:   true
deterministic Agent smoke:        verified
```

The final-machine validation is run provider-safe; Streamlit/OpenAI live status remains separately evidence-gated.

## Explicitly unverified / user-manual

- Streamlit live health-check on the actual presentation machine.
- OpenAI Responses live planner and provider latency/token/cost.
- GOAI portal review/upload/final submit and confirmation screenshot/email/receipt.
- Real timed pitch/demo/Q&A rehearsal and stability evidence.

No code presence, compile result, deterministic fallback, slide generation or local reviewer audit is treated as proof of these external/runtime/human items.
