# FitzSight v0.3 Validation

Date: 2026-08-11

## Automated tests

```text
pytest -q
19 passed, 1 skipped
```

The single skipped test remains the DuckDB-specific integration test because the build environment does not contain the `duckdb` package and has no package-network access. SQLite fallback validation is complete.

## Compile validation

```text
python -m compileall -q src scripts tests
PASS
```

## Deterministic investigation

Command used in the build environment:

```bash
python scripts/investigate.py --backend sqlite \
  --output docs/V0.3_SAMPLE_INVESTIGATION.json
```

Observed benchmark result:

| Signal | Result |
|---|---:|
| Affected Europe Team A+B FTD change | -7.53 pp |
| Europe control FTD change | -1.21 pp |
| Affected median response-time change | +29.15 min |
| Conversion p-value | ~0.00235 |
| Root-cause status | `supported_candidate` |
| Team contribution reconstruction error | ~2.2e-15 pp |
| Largest negative team contributor | Team A |
| Post-change high response-time anomaly days | 8 |
| Evidence records | 10 |
| Evidence-linked claims | 6 |

The Europe-wide FTD-rate decomposition ranks Team A and Team B as the two largest negative contributions in the default synthetic benchmark.

## Validation boundary

This build validates deterministic diagnostic behavior using the SQLite fallback. It does not claim DuckDB runtime validation in the build environment. The tracker task `T3` therefore remains `in_progress` until an external environment provides test/runtime evidence.
