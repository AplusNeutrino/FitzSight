# FitzSight v0.13.0 验证记录

## 发布边界

- `deepseek_live: not_requested`
- 真实 Provider 延迟：`not_run`
- 在线请求成功：不声明
- 发布证据：离线确定性运行、Provider Mock 请求契约、完整本地测试、静态扫描与 PDF QA

## 验收项

| 检查 | 结果 |
|---|---|
| Flash 默认与 Pro 切换 | PASS |
| 模型白名单 | PASS |
| 请求体、鉴权、JSON Output | PASS |
| 网络前意图拦截 | PASS |
| 空响应、非法 JSON、超时、HTTP 错误、截断 | PASS |
| 遥测字段与密钥脱敏 | PASS |
| 完整 Python 测试 | PASS · 98 passed |
| Python compileall | PASS |
| 确定性 Agent 冒烟 | PASS · verified |
| Provider 迁移静态扫描 | PASS · 0 hits |
| Streamlit 健康检查 | PASS · HTTP 200 / ok |
| 提交预检 | PASS · 12 页，0 secrets，0 legacy PPTX |
| 交接与最终机器包 | PASS · ZIP integrity |
| PDF 12 页 / 16:9 / 无溢出 | PASS |
| guizang 演讲者模式校验 | PASS |
| guizang Swiss 校验 | PASS（0 errors，仅模板导航安全区提示） |
| Poppler 全页渲染与逐页视觉检查 | PASS |

## 精确命令

```powershell
python -m compileall -q src scripts streamlit_app.py
python -m pytest -q
python scripts/check_provider_migration.py
python scripts/preflight_submission.py --output docs/V0.13_SUBMISSION_PREFLIGHT.json
python scripts/validate_streamlit_runtime.py --output docs/V0.13_STREAMLIT_RUNTIME.json
python scripts/final_machine_check.py --skip-streamlit --output docs/V0.13_FINAL_MACHINE_READINESS.json
```

最终机器报告：`local_core_ready=true`；`deepseek_live.status=not_requested`。任何未执行项保持 `not_run`，不以推测值替代。
